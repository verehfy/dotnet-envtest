name: Set up .NET SDK
description: Installs the .NET SDK version pinned in global.json.
runs:
  using: composite
  steps:
    - uses: actions/setup-dotnet@v6
      with:
        global-json-file: global.json
